Task category: **file search / refactor**

Find every place in the codebase that uses the deprecated helper (search broadly
by name and usage pattern), then rename it consistently across all call sites and
its definition. Verify nothing else references the old name afterward.
