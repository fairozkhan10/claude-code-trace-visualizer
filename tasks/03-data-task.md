Task category: **data task**

Given a CSV/JSON dataset in the project, load it, compute summary statistics
(row count, per-column types, missing-value counts, and the top categories of
one categorical column), and write the results to a small markdown report.
